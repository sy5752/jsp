서버 프로그램 구현 기술

흐름 제어(forward)와 페이지 모듈화(JSP : 액션 태그)

	jsp에서 action을 쓰지 않는 이유?

1. include(동적, 정적) - 사진 참고해서 표 만들기

	- 동적
	시점 : runtime
	대상 : 실행 결과

	- 정적

2. 에러 처리
	- 각 jsp페이지별 에러페이지 지정
	- 응답 상태 코드별/ 예외 타입별 에러페이지 지정
	- 에러 페이지 작성
	- 에러 페이지 우선순위
	
3. 에러처리 방법(우선순위)
	- jsp 지시자 : <%@ page errorPage="error.jsp"%> 잘안씀
	- 예외 타입별 설정 : web.xml
	
	- 응답 상태 코드별 설정 : web.xml

페이지 모듈화
 1. 동적	include
 	RequestDispatcher.include()
 	실행시점
 	
 2. 정적 include
 	<%@ include file=""/>
 	컴파일시점
 	
 에러 페이지
 1. jsp페이지별로 errorPage 속성에 에러 발생시 이동할 jsp파일 기술 (x)
 2. 에러코드(상태코드)별 예외 페이지 설정
 	web.xml 404, 500
 3. 예외 타입별 예외 페이지 설정
 
 	1. ArithmeticException이 발생되는 jsp파일을 하나 생성 webapp/jsp/ari.jsp
 	2. ArithmeticException이 발생되었을 때 응답을 대신 생성해 줄 webapp/error/type/ariError.jsp를 생성
 	3. web.xml에 예외 타입별 에러 페이지 설정
 	4. 서버 재시작
 	5. localhost/jsp/ari.jsp로 접속해서 arithmethcException 발생하게끔 유도하여
 	   설정한 에러페이지로 이동되는지 확인
 	
 	탐구 과제
 	
 	1. webapp/error/type/runError.jsp를 생성(텍스트는 본인 편한대로)
 	2. RuntimeException에러가 발생할 수 있는 jsp페이지 생성
 		webapp/jsp/run.jsp
 	3. web.xml에 runtimeException 예외에 대해 runError.jsp 페이지가 응답을 생성하도록 설정
 	4. localhost/jsp.run.jsp로 접속
 	
 	5. **** 어떤 에러가 나오는지 확인
 		runtomeException이 발생한 것을 eclipse console 로그를 통해 확인
 		결과 : runError.jsp에서 응답이 옴
 		
 	6. **** localhost/jsp/ari.jsp 접속시 어떤 여러 페이지로 이동하는지 확인
 		결과 : ariError.jsp에서 응답이 옴
 		 	
 	7. arithmeticException, runtimeException이 발생한 것은 서버 에러로 볼 수 있는데
 		500.html(jsp) 에러페이지에서 응답이 생성되지 않았는데
 		
 		<error-page> 엘레멘트를 기술한 순서가 응답에 영향을 미치는지 확인
 		==> error-page 엘레멘트 기술 순서가 영향을 미치지 않음
 			예외 타입 에러페이지 설정이 에러코드 여러페이지보다 우선
 		
 		<error-page> 엘레멘트를 기술한 순서가 에러 응답에 영향을 미치는지 확인

